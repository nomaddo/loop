int(int) f = prim f;

void sub (int x)
{
  // int[10] a;
  // f (a[0]);
  // f (a[1]);
  // f (a[2]);
  // f (a[3]);
  // f (a[4]);

  int[13][23] a;
  // a[2][2] = 400;
  // a[2][3] = 12;
  // a[3][3] = 100;
  // a[4][3] = 10000;
  int b = a[7][11];
}